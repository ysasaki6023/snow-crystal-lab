# ❄ 雪結晶ラボ — Snow Crystal Lab

気温・湿度・気圧・水分量を変えると結晶の形が劇的に変わる様子を眺める
3D 雪結晶成長シミュレータ。

## デモ

**https://ysasaki6023.github.io/snow-crystal-lab/** (GitHub Pages)

## ローカル起動

```bash
cd snow-crystal-sim
python3 -m http.server 8741
# → http://localhost:8741 を開く (Chrome / Safari / Edge)
```

ビルド不要・外部 CDN 不要 (three.js は vendor/ に同梱)。

## モデル (なるべく第一原理で、Web で動く程度に簡略化)

Gravner & Griffeath の 3D メソスコピック雪結晶モデル
(*Phys. Rev. E* **79**, 011601, 2009) をベースにした
六方格子 (横6近傍 + 縦2近傍) のセルオートマトン。

- **拡散**: 水蒸気場 d を離散拡散。付着セルは反射境界
- **凍結**: 結晶に接するセルで d → 境界準液層 b へ
- **付着**: b が閾値 β を超えたら結晶化。横 βH / 縦 βV の比が
  板状 ⇔ 柱状の晶癖を決める (温度依存 = 中谷ダイヤグラム)
- **融解**: b の一部が蒸気に戻る (準液層平衡 → ファセット形成)

物理パラメータの接続:

| UI | 物理 | モデルへの効き |
|---|---|---|
| 気温 | 晶癖の異方性 A(T) (中谷ダイヤグラム) | βV/βH 比 |
| 湿度 | 対氷過飽和 → 過剰水蒸気密度 (Magnus式) | 遠方場蒸気密度 ρ |
| 水分量 | 雲水量 (利用可能な水蒸気の上限) | ρ の頭打ち |
| 気圧 | 拡散係数 D ∝ 1/P | 拡散混合率 w |

実装上の工夫:

- **D6h 対称くさび格子**: 結晶の12回対称性を使い 30° の扇形だけを計算
  (12倍高速化 + 完全に対称な結晶)
- **枝分かれ**: 純粋な遠方場拡散律速 + キンク閾値 (kinkF) が
  面中央の準液層飽和値より上にあることで、ファセット不安定性
  (面の充填列が面中央で停止 → 角だけが伸びる) が起きる
- 描画は露出面のみの六角柱メッシュ + MeshPhysicalMaterial (transmission)

## 計算エンジン (WebGPU / CPU の 2 系統)

| | WebGPU エンジン (既定) | CPU フォールバック |
|---|---|---|
| 実装 | `js/gpu-sim.js` (WGSL compute) | `js/worker.js` + `js/sim-core.js` |
| 格子 | R=384 / **512** / 640 (選択可)、モバイル 256-384 | R=236 (モバイル 156) |
| セル数 | 950万〜3600万 | 250万 |
| 速度 | 数百〜5000 step/s | 数十〜数百 step/s |
| 対応 | Chrome / Edge / Safari 26+ / Firefox 141+ | 全モダンブラウザ |

WebGPU 版は拡散・凍結・付着・融解の全パスを WGSL compute shader で実行し、
モデル (式・閾値・実行順序) は CPU 版と厳密に同一
(`test/gpu-parity.mjs` で集合一致を検証)。実装の要点:

- **付着イベントのみ readback**: 巨大なフィールド (数百MB) は GPU に置いたまま、
  「どのセルがいつ付着したか」(数KB/batch) だけを CPU に戻す。
  CPU 側ミラーがメッシュ抽出・統計・領域境界の計算を担う
- **メッシュは 60° セクター + 6 インスタンス描画**: 抽出は mesh-worker が
  くさび + 鏡映の 2 像だけ生成し、three.js 側で 60°×k 回転の
  6 インスタンスとして描く (頂点メモリ 1/6)。継ぎ目セルは半透明の
  二重描画を避けるため別メッシュで正確に 1 回ずつ展開
- **バッチ実行**: 数十〜192 ステップを 1 つの command buffer に詰めて
  dispatch オーバーヘッドを償却 (バッチ長は実時間 ~30ms 狙いで適応制御)

## 操作

## 操作

- **プリセット**: 星型樹枝 / 扇形板 / 六角板 / 針 / 角柱 / 低温柱
- **計算解像度**: 高 (R=384) / 超高 (R=512, 既定) / 極 (R=640)。
  WebGPU 利用時のみ表示。高いほど細かい構造が出るが成長に時間がかかる
- **成長中にスライダーを動かせる** — 例: 角柱を1分育ててから
  気温を -15℃ にすると**冠角柱** (柱の両端に板) ができる
- 形態ダイヤグラム上のマーカーが現在の条件と予想形態を表示
- ドラッグで回転、ホイールでズーム、自動回転 ON/OFF

## 較正テスト (Node)

```bash
node test/tune.mjs 25000        # 6形態の分化チェック
node test/slice.mjs -15 140 1013 0.30 20000   # ASCII断面で目視
node test/gg2d.mjs 0.635 1.6 0.4 0.025 0.0025 0.015 0.0001 12000  # GG 2D 参照実装
```

## GPU エンジンテスト (要: ローカルサーバー + Chrome)

```bash
python3 -m http.server 8741 &
node test/gpu-parity.mjs 6000 500    # GPU/CPU の付着セル集合一致を検証
node test/gpu-perf.mjs 512 320 40    # R=512 の step/s 計測
node test/browser-gpu.mjs 30 /tmp/shot.png [preset]  # 実画面スモーク + screenshot
```
